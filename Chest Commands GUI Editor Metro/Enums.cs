namespace CCGE_Metro {
    public enum MinecraftStruct {
        Item,
        Enchantment
    }

    public enum IndexBase {
        ZeroBasedIndex,
        OneBasedIndex
    }

    public enum CutCopyMode {
        None,
        Cut,
        Copy
    }
}